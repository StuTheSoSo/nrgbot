{
  "name": "nrgbot",
  "displayName": "NRGBot",
  "description": "Private NRG Code Wizard",
  "version": "0.0.1",
  "publisher": "Parraid",
  "icon": "images/icon.png",
  "engines": {
    "vscode": "^1.90.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [],
  "main": "./out/extension.js",
  "contributes": {
    "viewsContainers": {
      "activitybar": [
        {
          "id": "ollama-sidebar-container",
          "title": "ParraidBot",
          "icon": "images/icon.png"
        }
      ]
    },
    "views": {
      "ollama-sidebar-container": [
        {
          "type": "webview",
          "id": "ollama.chatSidebarView",
          "name": "NRG Wizard",
          "icon": "$(comment-discussion)",
          "retainContextWhenHidden": true
        }
      ]
    },
    "commands": [
      {
        "command": "nrgbot.refreshProjectContext",
        "title": "Refresh Project Context",
        "category": "NRGBot"
      },
      {
        "command": "nrgbot.generateKnowledgeDoc",
        "title": "Generate Knowledge Doc",
        "category": "NRGBot"
      },
      {
        "command": "nrgbot.applyProposedEdit",
        "title": "Apply Change",
        "category": "NRGBot",
        "icon": "$(check)"
      },
      {
        "command": "nrgbot.discardProposedEdit",
        "title": "Discard Change",
        "category": "NRGBot",
        "icon": "$(close)"
      }
    ],
    "menus": {
      "editor/title": [
        {
          "command": "nrgbot.applyProposedEdit",
          "when": "nrgbot.hasPendingEdit && resourceScheme == 'nrgbot-preview'",
          "group": "navigation@1"
        },
        {
          "command": "nrgbot.discardProposedEdit",
          "when": "nrgbot.hasPendingEdit && resourceScheme == 'nrgbot-preview'",
          "group": "navigation@2"
        }
      ]
    },
    "configuration": {
      "title": "NRGBot Configuration",
      "properties": {
        "nrgbot.serverUrl": {
          "type": "string",
          "default": "http://192.168.3.142:11434",
          "description": "The base URL of your remote Ollama server."
        },
        "nrgbot.modelName": {
          "type": "string",
          "default": "qwen2.5-coder:14b",
          "description": "The exact model name as it appears when running 'ollama list' on your remote machine. A 14b+ tool-calling model is strongly recommended; 7b models often hallucinate tool results."
        },
        "nrgbot.enableTools": {
          "type": "boolean",
          "default": true,
          "description": "Allow the model to call read-only workspace tools (read file, list directory, search text). Requires a tool-calling-capable model."
        },
        "nrgbot.knowledgeFile": {
          "type": "string",
          "default": ".nrgbot/knowledge.md",
          "description": "Workspace-relative path to a hand-authored knowledge doc about this solution. Its contents are injected into the model's system prompt when present."
        },
        "nrgbot.includeProjectMap": {
          "type": "boolean",
          "default": true,
          "description": "Automatically inject a compact map of the open solution (.sln projects, languages, target frameworks) into the model's system prompt."
        },
        "nrgbot.projectContextMaxChars": {
          "type": "number",
          "default": 6000,
          "description": "Maximum characters of injected project context (knowledge doc and solution map each capped to this) to protect the model's context window."
        }
      }
    }
  },
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "pretest": "npm run compile && npm run lint",
    "lint": "eslint src",
    "test": "vscode-test"
  },
  "devDependencies": {
    "@types/mocha": "^10.0.10",
    "@types/node": "^26.6.0",
    "@types/vscode": "^1.90.0",
    "@vscode/codicons": "^0.0.46-24",
    "@vscode/test-cli": "^0.0.10",
    "@vscode/test-electron": "^2.4.1",
    "eslint": "^9.19.0",
    "typescript": "^5.7.3",
    "typescript-eslint": "^8.22.0"
  }
}
